---
description: 決まった1案を、1箇所ずつ直して仕上げる
---

# /fal-polish — 磨き起動

使い方：`/fal-polish` の後に直したい案を貼って起動する（引数なしでも可）。

1. `pinpoint-polish` スキル（`.agents/skills/pinpoint-polish/SKILL.md`）を読み込み、以後はその手順・形式・HARD-GATE に厳密に従うこと

2. 起動時の引数を「直す対象の本文」として扱う
   - **引数がある場合**：貼り直しを求めず、そのまま取り込みへ進む
   - **引数がない場合**：`brainstorm/` に既存セッションがないか確認する
     - **あれば**：「そのあらすじを直しますか？　それとも別の案を持ち込みますか？」と確認（**質問はこれ1つだけ**）
     - **なければ**：「直したい案を貼ってください」と1問だけ尋ねる

3. `/fal-brainstorm` から「磨き」で接続された場合は、**貼り付けを求めず**、いま扱っている案をそのまま持ち込み、
   `brainstorm/<セッション名>.md` の「絶対の制約」と「決まったこと」を引き継ぐこと

4. ⚠️ **磨き専用のファイルは作らないこと。**書き足す先は次の2本だけ
   - `brainstorm/<セッション名>_あらすじ.md` … 直した文はここを上書きする
   - `brainstorm/<セッション名>.md` … 決めたことは「決まったこと」へ、直した記録は「直したところ」へ（1件3行）
   - 状態ファイルが無ければ `.agents/skills/mystery-deep-brainstorm/session-template.md` から作る。**毎ターン更新すること**

5. ⚠️ **番号（①②③）は振らないこと。**直す場所は**短い名前**と**その一文をそのまま引くこと**で指す

6. ⚠️ **決めてもらう前に本文を書き換えないこと**（HARD-GATE）。**1ターン1箇所。**案は**違う方向へ4〜6個＋逆張り案1個**

7. 選び方は**自然文で受けること。**記法を強制しない（「1と4を混ぜて」「もっと短く」「別案」で通る）

8. 決めたら、その場で**「どこに響くか」を必ず書くこと。**とくに**差し替えた文のすぐ隣**（直前・直後・括弧書き・見出し）が
   古い前提のまま残っていないかを毎回読み直す。3つとも無いなら「どこにも響かない」と書く

9. あらすじの文を直したら、**その章の初読メモを見ること**（直した指摘は ✅ にして残す。⚠️ 消さない）

10. 終わるときは、通しで読んで**食い違い・重なり・声のブレを報告するだけ**にすること。
    ⚠️ **勝手に直さない。**⚠️ **ファイルを別フォルダへ移動しない**

11. **全出力は日本語。**ターミナル操作が必要な場合は UTF-8 強制を前置すること

12. **説明の言葉は、普段使いの言葉で書くこと**
    - 各案の `→` 1行・`直す場所`・`狙い`・⭐おすすめの理由・どこに響くか・質問文・状態ファイルの記述が対象
    - ⚠️ 意味がすぐ取れない業界語を使わない（発散／抵触／棚卸し／書き戻し／裁定 など）
    - ⚠️ **本文に入る案そのもの**（差し替える文・セリフ案・地の文の案）は対象外。人物と場面に合う言葉を選んでよい

13. 「**ブレストに戻す**」と言われたら `.agents/skills/mystery-deep-brainstorm/SKILL.md` を、
    「**トリック錬成**」と言われたら `.agents/skills/twist-trick-forge/SKILL.md` を読み込み、
    **いまの内容を持ち込んで**接続すること（⚠️ 貼り直させない／⚠️ 磨いた内容を捨てない）

14. **案が前に決めた設定に引っかかったら、設定のほうを疑って言うこと**
　　- 「どれくらい効いているか」（軽い／中くらい／重い）を推し量り、**⭐こう変えれば**、または**外しても他に響かない**まで書く
　　- ⭐ **ユーザーが確定した設定・✅決まったこと・絶対の制約でも言ってよい。**決めるのは本人
　　- ⚠️ **勝手に変えない・勝手に外さない。**⚠️ **質問は増やさない**（「その設定をこう変えて通す」を選択肢の1つに混ぜる）
　　- ⚠️⚠️ **設定に合わせて案を丸めるくらいなら、足かせとして言うこと**

15. **次の一手や選択肢を出すとき、ほかのやり方のほうが向いているなら、渡す選択肢を1つ入れること**
　　- 案を増やす・骨格を作り直す → `/fal-brainstorm`（`.agents/skills/mystery-deep-brainstorm/SKILL.md`）
　　- 仕掛け・伏線・二度目に効く手がかり → `/fal-twist`（`.agents/skills/twist-trick-forge/SKILL.md`）
　　- 決まった1案を1箇所ずつ詰める → `/fal-polish`（`.agents/skills/pinpoint-polish/SKILL.md`）
　　- ⚠️ **選ばれたときだけ渡る。**勝手に渡らない。⚠️ 1ターンに渡す選択肢は1つまで
　　- **渡すときは、いまの内容をそのまま持ち込む**（⚠️ 貼り直させない／⚠️ 新しい状態ファイルを作らない）。渡った先の最初のターンは `【いま】` にどこから来たかを書く

16. **面白さを先に置くこと**（これは作品の初期を助ける道具である）
　　- ⚠️⚠️ **設定に引っかかっても、案のほうを丸めないこと。**設定を変える案を、必ず選択肢に1つ入れること
　　- ⚠️ **設定を変えるなら、変わることをはっきり書くこと**＝**何が変わるか／それで何が困るか／どこが混乱するか**の3つ
　　- ⭐ **現実と合っているかの確かめは、決めたあとに回すこと。**案の段階で止めない（📋宿題の「確かめる」に1行足すだけ）
　　- ⚠️ ただし、状態ファイルの**「絶対の制約」は案の段階でも守ること**

17. **案が選ばれても、その場で全部を確定にしないこと**
　　- 選ばれた案を**3〜6個の部品**に割り、**チェックボックスで「決定にする／採らない／直す」を受け取ること**
　　- ⭐ **先頭に「全部このまま決定する」を置くこと。**1クリックで終われるようにする
　　- ⚠️ 選ばれた案が複数あるなら、**案ごとに分けて出すこと**（混ぜない）
　　- そのあと、**「さらに洗練させるか」を1問だけ訊くこと**（⚠️ 別のターンにする）

18. **一区切りついたら、次に何をするかを訊く前に、決まったことを全部書き出して確かめてもらうこと**
　　- 目安＝**決まったことが3件以上たまった／📋宿題が1件片づいた／章を1つ直した／別のやり方へ渡る直前**
　　- ⚠️ **前の整理から5ターン以上あいていること。**毎ターン出さない
　　- ⭐ これは「片づけて」とは別の**軽い確認**で、**AI のほうから出してよい**（1件1行＋チェックボックス）

19. **次に何をするかを訊くときは、チャットモードを必ず選択肢に入れること**
　　- チャットモード＝`【いま】``【受けた】`・案4〜6個＋逆張り案・1ターン1問**をやめて、ふつうに会話する状態**
　　- ⚠️ **状態ファイルの更新は続けること**（⭐ 戻ったときに話したことが消えないように）
　　- 入ったら `01_free_memo/` の3本を読むこと … `fal用_作品案メモ.md` ／ `fal用_雑メモ.md` ／ `fal用_機能修正用メモ.md`
　　- ⚠️ **メモの中身を勝手に議題にしないこと。**「こんなことが書いてありますが、触りますか」と一言だけ
　　- ⚠️⚠️ **`fal用_機能修正用メモ.md` を読んでも、ワークフローを勝手に直さないこと。**直すときは必ず確認する
　　- 戻り方＝`戻る` `ワークフローに戻る` `案を出して`。⚠️ **コマンドを打ち直させない。**⚠️ **新しい状態ファイルも作らない**

20. ⚠️ **`/fal-plot`（仮）は、まだ作っていない。**行き着く先はプロット（各話のあらすじと、登場人物が何をするかを具体的に書いたもの）で、
　　そこから小説を書く予定だが、**いまは渡さないこと**
